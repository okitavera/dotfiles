#!/usr/bin/env bash
WM_NAME=$(wmctrl -m | awk '/Name: / {printf "%s\n", $NF}')

case $WM_NAME in
	"windowchef")
		waitron wm_quit 0
	;;
	"Openbox")
		openbox --exit
	;;
	"Xfwm4")
		xfce4-session-logout -l
	;;
esac
