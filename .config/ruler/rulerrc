# automatically add all normal window to the first group
type="normal"
	waitron window_snap middle;cookers put_window

# because we can't recorgnize the window type of rxvt-unicode
name="urxvt"
	waitron window_snap middle;cookers put_window

# leave the dialog window and align them
type="dialog"
	waitron window_snap middle

